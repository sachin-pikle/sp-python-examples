# Fast API Example

```

cd fast-api

pyenv shell 3.14.2

python --version

python -m venv .venv

source .venv/bin/activate

which python

pip install --upgrade pip

pip install "fastapi[standard]"
```

```
fastapi dev main.py
```

```
source deactivate
```